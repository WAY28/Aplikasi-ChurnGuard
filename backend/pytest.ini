[pytest]
; Supaya `import database`, `import models`, `import main`, dst. (semua modul
; flat di backend/, bukan package) bisa di-resolve pytest terlepas dari cara
; pytest dijalankan (plain `pytest` maupun `python -m pytest`).
pythonpath = .
testpaths = tests
